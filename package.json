{
  "description": "This package.json file is only used for installing npm dependencies. But this is not an installable node package, but a WireCloud mashup. Take a look into src/config.xml for more details about this mashup",
  "devDependencies": {
    "bower": "^1.3.12",
    "grunt": "^1.0.1",
    "grunt-contrib-compress": "^1.4.3",
    "grunt-wirecloud": "^0.9.0",
    "gruntify-eslint": "^3.1.0",
    "wirecloud-config-parser": "^0.2.0"
  },
  "private": true
}
